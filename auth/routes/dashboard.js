exports.dashboard = {
  // console.log("inside dashboard module")
  // requireLogin,
  // function(req, res, next) {
  //   console.log("querying database inside module")
  //   Student.find({
  //     teacher: req.teacher._id
  //   }, function(err, students) {
  //     if (err) {
  //       console.log(err)
  //       res.render('dashboard.jade', {
  //         csrfToken: req.csrfToken(),
  //         error: err
  //       })
  //     } else {
  //       // Make the lessons taken look pretty
  //       students.forEach(function(student) {
  //         return student.lessons_taken = student.lessons_taken.map(function(x) {
  //           return x.toLocaleDateString()
  //         })
  //       });
  //       res.render('dashboard.jade', {
  //         csrfToken: req.csrfToken(),
  //         students: students
  //       })
  //     }
  //   }).sort({
  //     'last_name': 1
  //   })
  // }
}